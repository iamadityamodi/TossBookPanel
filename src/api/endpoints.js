const API_PREFIX = "/api/v1/tossbook";

export default {
  getAllBats: `${API_PREFIX}/getAllBats`,
  getwallet: `${API_PREFIX}/wallet`,
  getlogin: `${API_PREFIX}/login`,
  // add more endpoints here
};
